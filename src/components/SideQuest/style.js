import styled from "styled-components";

export const StyledSideQuest = styled.div`

    border-radius: 20px;
    background-color: var(--purple-blue);
    padding: 20px;
    margin: 20px;
    text-align: center;
    p{
        color: var(--white);
    }
    img{
        width: 100%;
        border-radius: 13px;
        filter: drop-shadow(6px 4px 4px rgba(0, 0, 0, 0.25));
        margin-top: 20px;
        margin-bottom: 20px;
    }

    h4{
        color: var(--red);
        font-weight: bold;
    }

`