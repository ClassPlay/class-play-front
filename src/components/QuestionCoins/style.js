import styled from 'styled-components';

export const StyledQuestionCoins = styled.div`
  display: flex;
  justify-content: space-between;

  align-items: center;

  width: 40px;
  height: 19px;

  padding: 5px;

  background-color: var(--white);
  border-radius: 10px;

  color: var(--purple-blue);
  font-weight: 600;
  font-size: 12px;
  line-height: 14px;

  img {
    width: 13px;
  }
`;
